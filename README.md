# Система поиска вакансий HeadHunter

Проект для поиска, сохранения и управления вакансиями с использованием API HeadHunter (HH.ru).

## Описание

Приложение предоставляет интерфейс командной строки для:
- Поиска вакансий через API HeadHunter по ключевому слову
- Сохранения найденных вакансий в файлы (JSON или CSV)
- Просмотра сохраненных вакансий
- Управления данными о вакансиях

## Возможности

- 🔍 **Поиск вакансий** - Загрузка вакансий с HeadHunter API по ключевому слову
- 💾 **Сохранение данных** - Поддержка двух форматов: JSON и CSV
- 📊 **Управление данными** - Загрузка, просмотр и очистка сохраненных вакансий
- 🎯 **Валидация данных** - Автоматическая обработка и валидация данных вакансий
- 🧪 **Высокое покрытие тестами** - Более 85% покрытия кода тестами

## Требования

- Python 3.8+
- pip (менеджер пакетов Python)

## Установка

1. Клонируйте репозиторий или скачайте проект:
```bash
git clone https://github.com/Onny-ch/CourseProject2.git
cd CourseProject2
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Использование

Запустите программу:
```bash
python main.py
```

После запуска:
1. Выберите формат для сохранения данных (JSON или CSV)
2. Используйте меню для работы с вакансиями:
   - **1** - Загрузить вакансии по ключевому слову
   - **2** - Просмотреть сохраненные вакансии
   - **3** - Очистить файл с вакансиями
   - **4** - Выйти из программы

### Пример использования

```
Добро пожаловать в систему поиска вакансий с HeadHunter!

Выберите формат для сохранения данных:
1 — JSON
2 — CSV
Введите номер (1 или 2): 1

Доступные действия:
1 — Загрузить вакансии по ключевому слову
2 — Просмотреть сохранённые вакансии
3 — Очистить файл с вакансиями
4 — Выйти из программы

Введите номер действия (1–4): 1
Введите ключевое слово для поиска вакансий: Python

Загружаем вакансии по запросу «Python»...
Найдено 100 вакансий.
```

## Структура проекта

```
CourseProject2/
├── main.py                 # Точка входа, CLI интерфейс
├── requirements.txt       # Зависимости проекта
├── README.md              # Документация
├── src/                   # Исходный код
│   ├── __init__.py
│   ├── hh_api.py         # Класс для работы с HeadHunter API
│   ├── fileworker.py     # Классы для работы с файлами (JSON/CSV)
│   ├── vacancy.py        # Модель данных вакансии
│   └── services.py       # Вспомогательные функции
└── tests/                 # Тесты
    ├── __init__.py
    ├── test_hh_api.py    # Тесты для HeadHunter API
    ├── test_fileworker.py # Тесты для работы с файлами
    ├── test_vacancy.py   # Тесты для модели вакансии
    └── test_services.py   # Тесты для вспомогательных функций
```

## Архитектура

Проект следует принципам SOLID и использует:

- **Абстрактные базовые классы (ABC)**: `Parser` для парсеров вакансий, `AbstractFileWorker` для работы с файлами
- **Полиморфизм**: Различные реализации файловых воркеров (`JSONFileWorker`, `CSVFileWorker`)
- **Инкапсуляция**: Приватные атрибуты и методы в классах
- **Модульность**: Разделение ответственности между модулями

### Основные классы

- **`Vacancy`** - Модель вакансии с валидацией данных, поддержкой сравнения и форматирования
- **`HeadHunterAPI`** - Парсер для работы с API HeadHunter (наследует `Parser`)
- **`JSONFileWorker`** - Работа с JSON файлами (наследует `AbstractFileWorker`)
- **`CSVFileWorker`** - Работа с CSV файлами (наследует `AbstractFileWorker`)

## Тестирование

Проект включает комплексные unit-тесты с использованием `pytest`.

Запуск тестов:
```bash
pytest
```

Запуск тестов с проверкой покрытия:
```bash
pytest --cov=src --cov-report=term-missing
```

Текущее покрытие кода: **более 85%**

## Зависимости

Основные зависимости:
- `requests==2.32.5` - HTTP библиотека для работы с API
- `pytest==8.4.2` - Фреймворк для тестирования
- `pytest-cov==7.0.0` - Плагин для проверки покрытия кода

Инструменты разработки:
- `black==25.9.0` - Форматирование кода
- `flake8==7.3.0` - Линтер
- `isort==7.0.0` - Сортировка импортов

## Особенности реализации

- **Обработка ошибок**: Комплексная обработка ошибок для файловых операций, API запросов и валидации данных
- **Поддержка форматов**: Автоматическое определение формата данных (сырые API данные vs. сохраненные)
- **Оптимизация памяти**: Использование `__slots__` в классе `Vacancy` для уменьшения потребления памяти
- **Типизация**: Использование type hints для улучшения читаемости и поддержки кода
- **Валидация**: Автоматическая валидация и очистка данных (HTML, URL, названия)

## Формат данных

Вакансии сохраняются со следующими полями:
- `id` - Уникальный идентификатор
- `title` - Название вакансии
- `url` - Ссылка на вакансию
- `employer_name` - Название работодателя
- `employer_url` - Ссылка на работодателя
- `salary_from`, `salary_to` - Диапазон зарплаты
- `currency` - Валюта
- `gross` - Зарплата до вычета налогов (boolean)
- `responsibilities` - Обязанности
- `requirements` - Требования
- `professional_roles` - Профессиональные роли
- `experience` - Опыт работы
- `probation_period` - Испытательный срок
- `city`, `street`, `building` - Адрес

## Лицензия

Этот проект создан в учебных целях.

## Автор

Проект выполнен в рамках курсовой работы.
